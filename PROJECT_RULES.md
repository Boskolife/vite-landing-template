# 📋 Правила проекта vite-landing-template

## 🎯 Обязательные правила

### 1. **Документация и мультиязычность**

- ✅ **Вся документация** должна вестись на **украинском** и **английском** языках
- ✅ **Все гайды и инструкции** - украинский + английский языки
- ✅ **Все существующие файлы документации** - обновить до мультиязычности
- ✅ **Все новые файлы документации** - создавать с мультиязычностью
- ✅ **Дублирование контента** на двух языках
- ✅ **Приоритет:** сначала украинский, затем английский
- ✅ **Комментарии в документации** - на украинском и английском языках
- ✅ **Структуры файлов и папок** - комментарии на двух языках
- ✅ **Примеры кода в документации** - комментарии на двух языках
- ✅ **Заголовки и описания** - на двух языках
- ✅ **Списки и таблицы** - на двух языках

### 2. **Пути в HTML**

- ✅ **Ресурсы (изображения, стили, скрипты)** - начинаются с `./`
- ✅ Пример: `./images/logo.png`, `./styles/main.css`
- ✅ **Ссылки на страницы** - без `./` в начале
- ✅ Пример: `href="about.html"`, `href="contact.html"`
- ✅ **Главная страница** - `href="/"` или `href="./"`
- ❌ Не использовать абсолютные пути `/images/` для ресурсов
- ❌ Не использовать `./` для ссылок на страницы

### 3. **Комиты**

- ✅ Все комиты для GitHub должны писаться только на **английском** языке

### 4. **Комментарии в коде (не документация)**

- ✅ **Комментарии в коде** (не касающиеся документации) - только на **английском** языке
- ✅ **Однострочные комментарии** `//` в коде - только на английском
- ✅ **Многострочные комментарии** `/* */` в коде - только на английском
- ✅ **JSDoc комментарии** `/** */` в коде - только на английском
- ✅ **Комментарии в SCSS** `//` и `/* */` в коде - только на английском
- ✅ **Комментарии в HTML** `<!-- -->` в коде - только на английском
- ✅ **Комментарии в конфигурационных файлах** - только на английском
- ✅ **Комментарии в JavaScript** (логика, функции, алгоритмы) - только на английском

## 📝 Примеры

### Документация (мультиязычность):

```markdown
# WebP Optimization / WebP Оптимізація

## Features / Особливості

- Automatic conversion / Автоматична конвертація
- Fallback support / Підтримка fallback

## Quick Start / Швидкий старт

### Installation / Встановлення

1. Clone repository / Клонуйте репозиторій
2. Install dependencies / Встановіть залежності
3. Run development server / Запустіть сервер розробки

## Configuration / Конфігурація

| Option / Опція | Description / Опис                | Default / За замовчуванням |
| -------------- | --------------------------------- | -------------------------- |
| `quality`      | Image quality / Якість зображення | `80`                       |
| `generateWebp` | Generate WebP / Генерувати WebP   | `true`                     |
```

### Комментарии в структурах файлов:

```
src/styles/
├── main.scss              # Main import file / Головний файл імпортів
├── base/                  # Base styles / Базові стилі
│   ├── _variables.scss    # Variables / Змінні
│   └── _mixins.scss       # Mixins / Міксини
```

### Комментарии в коде (только английский, не документация):

#### JavaScript (логика, функции, алгоритмы):

```javascript
// Check WebP support in browser
export const isWebPSupported = () => {
  // Implementation here
};

/**
 * Initialize WebP support for the application
 * @param {string} className - CSS class name to add
 */
export const addWebPClass = (className = 'webp') => {
  /* Check browser support */
  isWebPSupported().then(supported => {
    // Add class if supported
  });
};
```

#### SCSS:

```scss
// Simple mixin for background image
@mixin bg-image($image-path) {
  background-image: url('#{$image-path}');
}

/* Hero section with background image */
@mixin hero-bg($image-path, $height: 100vh) {
  position: relative;
  height: $height;

  /* Overlay for text readability */
  &::before {
    content: '';
    background: rgba(0, 0, 0, 0.4);
  }
}
```

#### HTML:

```html
<!-- Main navigation menu -->
<nav class="main-nav">
  <!-- Navigation items -->
  <ul class="nav-list">
    <li><a href="/">Home</a></li>
    <li><a href="about.html">About</a></li>
  </ul>
</nav>
```

### HTML пути:

```html
<!-- ✅ Правильно -->
<!-- Ресурсы (изображения, стили, скрипты) -->
<img src="./images/logo.png" alt="Logo" />
<link rel="stylesheet" href="./styles/main.css" />
<script src="./js/main.js"></script>

<!-- Ссылки на страницы -->
<nav class="main-nav">
  <ul class="nav-list">
    <li><a href="/">Home</a></li>
    <li><a href="about.html">About</a></li>
    <li><a href="contact.html">Contact</a></li>
  </ul>
</nav>

<!-- ❌ Неправильно -->
<!-- Абсолютные пути для ресурсов -->
<img src="/images/logo.png" alt="Logo" />
<link rel="stylesheet" href="/styles/main.css" />

<!-- Неправильные ссылки -->
<a href="./about.html">About</a>  <!-- Не нужно ./ для ссылок -->
```

## 🔄 Применение

### Мультиязычность (украинский + английский):

- 📄 **Вся документация** (Markdown, README, гайды, инструкции)
- 📚 **Все гайды и руководства** (guides/, technologies/, examples/)
- 📝 **Все существующие файлы документации** (обновить до мультиязычности)
- 🆕 **Все новые файлы документации** (создавать с мультиязычностью)
- 📋 **Все заголовки, описания, списки, таблицы**
- 🔗 **Ссылки и пути в документации**

### Только английский язык:

- 💻 **Комментарии в коде** (логика, функции, алгоритмы)
- 🎨 **Комментарии в SCSS** (миксины, функции, переменные)
- 🌐 **Комментарии в HTML** (структура, логика)
- ⚙️ **Комментарии в конфигурационных файлах**
- 🔧 **Коммиты Git**
- 📛 **Названия переменных и функций**
- 🔤 **Технические термины**

### HTML пути:

- 📁 **Ресурсы** (изображения, стили, скрипты) - начинаются с `./`
- 🔗 **Ссылки на страницы** - без `./` в начале
- 🏠 **Главная страница** - `href="/"` или `href="./"`

## 🚫 Что НЕ переводится (только английский)

- **Коммиты Git** - только на английском
- **Комментарии в коде** (логика, функции, алгоритмы) - только на английском
- **Названия переменных и функций** - только на английском
- **Технические термины** - остаются на английском
- **Названия файлов и папок** - только на английском
- **API endpoints и URL** - только на английском
- **Конфигурационные ключи** - только на английском

## ✅ Что переводится (украинский + английский)

- **Вся документация** (Markdown, README, гайды, инструкции)
- **Все заголовки и описания** в документации
- **Все списки и таблицы** в документации
- **Все примеры кода** в документации
- **Все комментарии** в документации
- **Все структуры файлов** в документации

## 📋 Примеры мультиязычности для разных типов файлов

### README файлы:

```markdown
# Project Name / Назва проекту

## Description / Опис

This is a modern landing page template / Це сучасний шаблон лендінгової сторінки

## Features / Особливості

- ⚡ Fast build / Швидка збірка
- 🎨 Modern design / Сучасний дизайн
- 📱 Responsive / Адаптивний
```

### Гайды и инструкции:

```markdown
# Quick Start Guide / Керівництво швидкого старту

## Installation / Встановлення

1. Clone repository / Клонуйте репозиторій
2. Install dependencies / Встановіть залежності
3. Run development server / Запустіть сервер розробки

## Usage / Використання

Follow these steps / Дотримуйтесь цих кроків
```

### Техническая документация:

```markdown
# API Reference / Довідник API

## Methods / Методи

### `getUser(id)` / `getUser(id)`

Returns user data / Повертає дані користувача

**Parameters / Параметри:**

- `id` (string) - User ID / ID користувача

**Returns / Повертає:**

- `Promise<User>` - User object / Об'єкт користувача
```

### Примеры кода в документации:

```javascript
// Initialize application / Ініціалізувати додаток
function initApp() {
  // Setup event listeners / Налаштувати обробники подій
  document.addEventListener('DOMContentLoaded', () => {
    console.log('App loaded / Додаток завантажено');
  });
}
```

## ✅ Чек-лист соответствия правилам

### Документация (мультиязычность):

- [ ] Все заголовки имеют перевод `# Title / Заголовок`
- [ ] Все описания имеют перевод `Description / Опис`
- [ ] Все списки имеют перевод `- Item / Елемент`
- [ ] Все таблицы имеют перевод `| Column / Колонка |`
- [ ] Все комментарии в документации имеют перевод `// Comment / Коментар`
- [ ] Все структуры файлов имеют перевод `# File / Файл`

### Код (только английский):

- [ ] Комментарии в JavaScript на английском
- [ ] Комментарии в SCSS на английском
- [ ] Комментарии в HTML на английском
- [ ] Комментарии в конфигурационных файлах на английском
- [ ] Названия переменных и функций на английском
- [ ] Технические термины на английском

### Git и общее:

- [ ] Коммиты на английском языке
- [ ] HTML пути для ресурсов начинаются с `./`
- [ ] HTML ссылки на страницы без `./` в начале
- [ ] Все новые файлы документации создаются с мультиязычностью
- [ ] Все существующие файлы документации обновлены до мультиязычности

## 🎯 Итоговые правила

1. **Документация** = украинский + английский
2. **Код** = только английский
3. **Git** = только английский
4. **HTML ресурсы** (изображения, стили, скрипты) = начинаются с `./`
5. **HTML ссылки** на страницы = без `./` в начале
6. **Приоритет языков** = сначала украинский, затем английский
